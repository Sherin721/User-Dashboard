# 🧑‍💼 MERN User Dashboard (with PostgreSQL)

A full-stack **User Dashboard Application** built using the **MERN Stack (MongoDB replaced with PostgreSQL)**.  
This app allows user authentication, profile management, and admin dashboard features — built with modern web technologies and clean architecture.

---

## 🚀 Features

- 🔐 User authentication (Register / Login / Logout)
- 🧑‍💼 Role-based access (Admin & Regular users)
- 📊 Dashboard with user statistics and analytics
- ✏️ Edit / Update user profile data
- 🧠 State management using **Redux Toolkit**
- 🧩 REST API with **Express.js** and **PostgreSQL**
- 🎨 Responsive React UI with reusable components
- 🌐 API communication handled via **Axios**

---

## 🏗️ Tech Stack

### **Frontend**
- React.js (CRA or Vite)
- Redux Toolkit
- React Router DOM
- Axios
- TailwindCSS / Bootstrap (optional for styling)

### **Backend**
- Node.js
- Express.js
- PostgreSQL (via `pg` npm package)
- bcrypt.js (for password hashing)
- jsonwebtoken (JWT Auth)
- dotenv (for environment variables)

---

## ⚙️ Installation & Setup

### 1️⃣ Clone the repository
```bash
git clone https://github.com/YourUsername/mern-dashboard.git
cd mern-dashboard
